spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vineethdb1
    username: root
    password: Root
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
      
    show-sql: true
    
server:
  ssl:
    key-alias: spring-https
    Key-store: classpath:vineeth.jks
    Key-store-type: JKS
    Key-password: password
    